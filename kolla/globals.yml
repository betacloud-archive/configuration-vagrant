---
##########################################################
# ansible

ansible_become: true
ansible_become_user: root

##########################################################
# networking

network_interface: enp0s8

neutron_external_interface: enp0s9

storage_interface: enp0s10
cluster_interface: enp0s10

##########################################################
# kolla

config_strategy: COPY_ALWAYS
node_custom_config: /config/overlays

kolla_base_distro: ubuntu
kolla_install_type: source

kolla_internal_vip_address: 169.254.169.10
kolla_external_vip_address: 169.254.169.20

openstack_release: ocata

##########################################################
# docker

docker_namespace: betacloud
docker_restart_policy_retry: 0

##########################################################
# openstack

# enable services
enable_ceilometer: "yes"
enable_ceph: "yes"
enable_cinder: "yes"
enable_heat: "yes"

# generic parameters
enable_central_logging: "yes"

openstack_logging_debug: "True"
openstack_service_workers: 2
openstack_region_name: vagrant

# ceilometer
ceilometer_database_type: mysql

# ceph
ceph_osd_filesystem: "xfs"

# horizon
horizon_backend_database: "yes"

# keystone
keystone_token_provider: "fernet"

# neutron
neutron_plugin_agent: "openvswitch"
enable_neutron_qos: yes

# nova
nova_console: novnc
